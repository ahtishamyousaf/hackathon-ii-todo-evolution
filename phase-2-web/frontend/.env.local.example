# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000

# Better Auth Configuration
BETTER_AUTH_SECRET=your-better-auth-secret-must-match-backend-use-secrets-token-urlsafe-32

# OpenAI ChatKit Configuration (Phase III)
NEXT_PUBLIC_CHATKIT_DOMAIN_KEY=domain_pk_localhost_dev

# Note: BETTER_AUTH_SECRET must be identical in both frontend and backend
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
#
# Note: For production, register your domain at:
# https://platform.openai.com/settings/organization/security/domain-allowlist
# and replace domain_pk_localhost_dev with your real domain key
