[project]
name = "hackathon-2"
version = "0.1.0"
description = "Todo Console App - Phase I with Spec-Driven Development"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=7.4.0",
]
